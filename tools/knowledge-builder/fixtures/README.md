# Fixtures

`local-context.json` é o contexto mínimo de uma build integral local. As
fixtures de conteúdo dos testes são derivadas da própria fonte canônica e
materializadas apenas em diretórios temporários; assim, nenhuma cópia do
catálogo se torna uma segunda fonte de verdade.

//! Keeps media inventory ownership explicit within the inventory boundary.

// Media obligations are emitted by the root inventory traversal because they
// depend only on the locale-wide validated media index.
